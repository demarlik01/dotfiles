interface:
  display_name: "Claude Session Handoff"
  short_description: "Resume work from local Claude session logs"
  default_prompt: "Use $claude-session-handoff to find my relevant local Claude session and continue the unfinished work."
